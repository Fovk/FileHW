<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
     TextElement.Foreground="{DynamicResource MaterialDesignBody}"
     TextElement.FontWeight="Regular"
     TextElement.FontSize="13"
     TextOptions.TextFormattingMode="Ideal"
     TextOptions.TextRenderingMode="Auto"
     Background="{DynamicResource MaterialDesignPaper}"
     FontFamily="{DynamicResource MaterialDesignFont}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Grid>
        <materialDesign:Card Padding="32" Margin="31,27,33,0" Height="341" Foreground="#DDF50000">
            <materialDesign:Card.Effect>
                <DropShadowEffect BlurRadius="10" Color="#FFA65252"/>
            </materialDesign:Card.Effect>
            <TextBlock x:Name="Student" Style="{DynamicResource MaterialDesignTitleTextBlock}" Margin="0,0,-14,-3"></TextBlock>
        </materialDesign:Card>
    </Grid>
</Window>
